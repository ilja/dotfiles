##### Installation

Clone repo in home directory

* `cd dotfiles`

Init and update dotvim repo

* `git submodule init`
* `git submodule update`

Create symlinks

* `./install.sh`

Build commant-T

* `cd dotvim`
* `make`